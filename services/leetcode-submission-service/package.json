{
  "name": "leetcode-submission-service-ts",
  "version": "0.2.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "rm -rf dist && tsc -p tsconfig.json",
    "test": "node --import tsx --test tests/codeCleaner.test.ts tests/database.test.ts",
    "submission-server": "node dist/server.js",
    "submission-server-dev": "tsx watch src/ts/server.ts"
  },
  "dependencies": {
    "@prisma/client": "^5.22.0",
    "pino": "^9.14.0",
    "tsx": "^4.19.2"
  },
  "devDependencies": {
    "@types/node": "^22.10.1",
    "prisma": "^5.22.0",
    "typescript": "^5.7.2"
  }
}
