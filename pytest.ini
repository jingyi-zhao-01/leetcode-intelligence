[pytest]
# Test discovery patterns
testpaths = packages
pythonpath = 
    packages/submission_server/src
    packages/mcp-server/src
python_files = test_*.py *_test.py
python_classes = Test*
python_functions = test_*

# Output options
addopts = 
    -v
    --tb=short
    --strict-markers
    --disable-warnings

# Markers
markers =
    unit: Unit tests
    integration: Integration tests
    slow: Slow running tests
